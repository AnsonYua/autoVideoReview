[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "grok-workflow"
version = "0.1.0"
description = "Local orchestrator for Grok Web review and ComfyUI video generation."
requires-python = ">=3.11"
dependencies = ["playwright>=1.52.0"]
